Sum of Divisor from 1 to n 

void sum_of_divisor()
{
    for (int i = 1; i * i <= n; i++)
    {
        for (int j = i * i; j < n; j += i)
        {
            if (j == i * i)
                a[j] += i;
            else
                a[j] += I + (j / i);
        }
    }
}

